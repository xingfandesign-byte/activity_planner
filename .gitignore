# Exclude all files and directories starting with .
.*

# But keep .gitignore itself
!.gitignore

# SQLite database (local data)
backend/activity_planner.db
backend/*.db

# Python
backend/venv/
backend/__pycache__/
